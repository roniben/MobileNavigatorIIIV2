<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/add_places_drawer"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    android:padding="@dimen/constraint_pad"
    tools:context=".addPlaces"
    tools:openDrawer="start">


    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="CHOOSE IMAGE"
        android:layout_centerHorizontal="true"
        android:textSize="25sp"
        android:layout_marginBottom="10dp"
        android:gravity="center_horizontal"
        android:id="@+id/xml_title"
        android:textColor="@android:color/white"
        android:background="@android:color/holo_green_dark"/>


    <Button
        android:id="@+id/choose_image_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/upload_image_view"
        android:layout_marginTop="20dp"
        android:gravity="center"
        android:background="@android:color/transparent"
        android:layout_toStartOf="@id/next_button"
        android:text="Choose file" />

    <EditText
        android:id="@+id/enter_place_name"
        android:layout_width="500dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/xml_title"
        android:hint="Enter Place Name"
        android:layout_centerHorizontal="true"/>

    <ImageView
        android:id="@+id/upload_image_view"
        android:layout_width="match_parent"
        android:layout_height="350dp"
        android:layout_below="@id/enter_place_name"
        android:layout_marginTop="5dp"
        android:layout_centerHorizontal="true"/>

    <Button
        android:id="@+id/next_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/upload_image_view"
        android:layout_marginTop="20dp"
        android:background="@android:color/transparent"
        android:layout_alignRight="@id/upload_image_view"
        android:layout_marginLeft="5dp"
        android:text="Next" />


</RelativeLayout>